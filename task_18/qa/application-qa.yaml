apiVersion: argoproj.io/v1alpha1

kind: Application

metadata:

  name: payment-qa

  namespace: argocd

spec:

  project: default

  source:

    repoURL: https://github.com/<username>/gitops-platform.git

    targetRevision: main

    path: qa

  destination:

    server: https://kubernetes.default.svc

    namespace: qa

  syncPolicy:

    automated:

      prune: true

      selfHeal: true

    syncOptions:

      - CreateNamespace=true